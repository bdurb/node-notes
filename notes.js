console.log('starting notes');

const addNote = (title, body ) => {

};

const getAll = () => {

};

const getNote = (title) => {

};

const removeNote = () => {

};

module.exports = {
  addNote,
  getAll,
  getNote
}