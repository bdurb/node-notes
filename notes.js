console.log('starting notes');

const addNote = (title, body ) => {

};

const getAll = () => {

};

const getNote = (title) => {

};

const removeNote = (title) => {

};

module.exports = {
  addNote,
  getAll,
  getNote,
  removeNote
}